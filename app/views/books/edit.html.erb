<h1>Editing Book</h1>
<%= form_with model:@book, local:true do |f| %>

<p>title</p>
<%= f.text_field :title %>
<p>Body</p>
<%= f.text_area :body %>
<%= f.submit 'Update Book' %>
<% end %>

<%= link_to "Edit", check_book_path(@book) %>
"|"
<%= link_to "Back", index_book_path(@book) %>