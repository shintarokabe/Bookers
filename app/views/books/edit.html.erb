<h1>Editing Book</h1>
<%= form_with model:@book, local:true do |f| %>

<div>title</div>
<%= f.text_field :title %>
<div>Body</div>
<%= f.text_area :body %>
<div>
<%= f.submit 'Update Book' %>
</div>
<% end %>

<div><%= link_to "Show", book_path(@book) %>
 |
<%= link_to "Back", new_book_path(@books) %>
</div>